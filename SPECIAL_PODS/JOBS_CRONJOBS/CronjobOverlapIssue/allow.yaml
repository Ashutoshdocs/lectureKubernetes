apiVersion: batch/v1
kind: CronJob
metadata:
  name: cron-allow
spec:
  schedule: "*/1 * * * *"
  concurrencyPolicy: Allow
  successfulJobsHistoryLimit: 2
  failedJobsHistoryLimit: 1

  jobTemplate:
    spec:
      template:
        spec:
          restartPolicy: Never

          containers:
          - name: demo
            image: busybox
            command:
            - sh
            - -c
            - |
              echo "Started: $(date)"
              sleep 120
              echo "Finished: $(date)"