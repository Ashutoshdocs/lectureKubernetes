apiVersion: v1
kind: Service

metadata:
  name: nginx-emptydir-service

spec:

  type: NodePort

  selector:
    app: nginx-emptydir

  ports:

  - protocol: TCP
    port: 80
    targetPort: 80
    nodePort: 30080