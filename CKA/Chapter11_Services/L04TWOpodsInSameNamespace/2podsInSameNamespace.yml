apiVersion: v1
kind: Namespace
metadata:
  name: demo
---
apiVersion: v1
kind: Pod
metadata:
  name: pod1
  namespace: demo
  labels:
    app: pod1
spec:
  containers:
  - name: nginx
    image: nginx:latest
    command:
    - /bin/sh
    - -c
    - |
      echo "<h1>Hit Pod1</h1>" > /usr/share/nginx/html/index.html
      nginx -g 'daemon off;'
    ports:
    - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: pod1-service
  namespace: demo
spec:
  type: NodePort
  selector:
    app: pod1
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30080
---
apiVersion: v1
kind: Pod
metadata:
  name: pod2
  namespace: demo
  labels:
    app: pod2
spec:
  containers:
  - name: nginx
    image: nginx:latest
    command:
    - /bin/sh
    - -c
    - |
      echo "<h1>Hit Pod2</h1>" > /usr/share/nginx/html/index.html
      nginx -g 'daemon off;'
    ports:
    - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: pod2-service
  namespace: demo
spec:
  type: NodePort
  selector:
    app: pod2
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30081