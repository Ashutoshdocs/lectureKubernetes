apiVersion: v1
kind: Service
metadata:
  name: rsv2-service
  labels:
    app: nginx-rs
    version: rsv2
spec:
  type: NodePort
  selector:
    app: nginx-rs
    version: rsv2
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 30082
