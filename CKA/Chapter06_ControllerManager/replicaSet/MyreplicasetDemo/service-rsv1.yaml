apiVersion: v1
kind: Service
metadata:
  name: rsv1-service
  labels:
    app: nginx-rs
    version: rsv1
spec:
  type: NodePort
  selector:
    app: nginx-rs
    version: rsv1
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 30081
