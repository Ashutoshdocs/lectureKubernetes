apiVersion: v1
kind: Service
metadata:
  name: nginx-rs-service
spec:
  type: NodePort
  selector:
    app: nginx-rs
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 30081
