apiVersion: v1
kind: Service
metadata:
  name: resilience-demo
  labels:
    app: resilience-demo
spec:
  type: ClusterIP
  selector:
    app: resilience-demo
  ports:
    - name: http
      port: 80
      targetPort: 8080
