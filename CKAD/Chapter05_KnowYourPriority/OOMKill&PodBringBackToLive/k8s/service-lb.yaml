apiVersion: v1
kind: Service
metadata:
  name: resilience-demo-lb
  labels:
    app: resilience-demo
spec:
  type: LoadBalancer
  selector:
    app: resilience-demo
  ports:
    - name: http
      port: 80
      targetPort: 8080
