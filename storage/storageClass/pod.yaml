apiVersion: v1
kind: Pod

metadata:
  name: storage-demo

spec:

  containers:

  - image: nginx

    name: nginx

    volumeMounts:

    - mountPath: /data

      name: storage

  volumes:

  - name: storage

    persistentVolumeClaim:

      claimName: azure-pvc