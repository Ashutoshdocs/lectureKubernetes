apiVersion: v1
kind: Service

metadata:
  name: greenpulse

spec:
  selector:
    app: greenpulse

  ports:
    - name: http
      port: 80
      targetPort: 8080
      nodePort: 30081

  type: NodePort