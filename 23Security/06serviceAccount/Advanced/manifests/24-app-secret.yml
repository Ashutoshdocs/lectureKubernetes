# A Secret that exists only to demonstrate a denial: the reporter has no RBAC
# permission on secrets at all, so any attempt to read this fails with 403.
apiVersion: v1
kind: Secret
metadata:
  name: app-secret
  namespace: acme-prod
  labels:
    app.kubernetes.io/part-of: serviceaccount-demo
type: Opaque
stringData:
  api-key: "s3cr3t-do-not-leak"
