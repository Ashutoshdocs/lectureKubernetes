# The identity our "powerful" pod will run as.
# A ServiceAccount on its own has NO permissions -- it only gains them once a
# Role is bound to it (see 03-rolebinding.yml).
apiVersion: v1
kind: ServiceAccount
metadata:
  name: pod-creator
  namespace: practical
  labels:
    demo: rbac-serviceaccount
