apiVersion: v1
kind: Pod
metadata:
  name: webapp
  namespace: dev
  labels:
    app: webapp
spec:
  containers:
  - name: webapp
    image: hashicorp/http-echo
    args:
    - "-text=Hello From DEV"
---
apiVersion: v1
kind: Service
metadata:
  name: web-service
  namespace: dev
spec:
  selector:
    app: webapp
  ports:
  - port: 5678
    targetPort: 5678